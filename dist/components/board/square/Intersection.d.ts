import { FC } from "react";
import { PointPosition } from "../SvgBoard";
declare type Props = {
    cx: number;
    cy: number;
    width: number;
    pointPosition: PointPosition;
};
export declare const Intersection: FC<Props>;
export {};
