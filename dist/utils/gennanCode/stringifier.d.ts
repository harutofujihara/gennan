import { GennanCore, Point } from "gennan-core";
declare function stringifyGennanCode(params: {
    gnc?: GennanCore;
    fulcrumPoint?: Point;
    sideCount?: number;
}): string;
export { stringifyGennanCode };
